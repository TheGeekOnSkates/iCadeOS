 ORG $D100	/* for now */
 PROCESSOR 6502
 include "iCadeOS.s"

MAIN:
	JSR READ_DPAD
	JSR READ_BUTTONS
	JMP MAIN

READ_DPAD:
	LDA INPUT_DPAD
	STA SCREEN_RAM
	RTS

READ_BUTTONS:
	LDA INPUT_BUTTONS
	STA SCREEN_RAM + 1
	RTS
